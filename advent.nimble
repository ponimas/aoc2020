# Package

version       = "0.1.0"
author        = "Aleksandr Ponimaskin"
description   = "A new awesome nimble package"
license       = "MIT"
srcDir        = "src"
bin           = @["advent"]
# binDir      = "target"

# Dependencies

requires "nim >= 1.4.0"
requires "unpack >= 0.4.0"
requires "memo >= 0.3.0"
